<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Light only, matching the desktop app — the "Notion warm paper" system
         has no dark mode. Warm #f6f5f4 window background so there's no flash
         before Compose draws. -->
    <style name="Theme.FamilyAgent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
